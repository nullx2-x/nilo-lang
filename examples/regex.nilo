import "std/regex" as regex;

let email = regex.captures(
    "(?P<user>[\\w.]+)@(?P<host>[\\w.]+)",
    "dev@example.com"
);

print(email.named.user);
print(email.named.host);
print(regex.find_all("\\w+", "Nilo speaks many languages"));
print(regex.replace("\\s+", "Nilo Language", "-"));
